{
  "name": "@trusty-squire/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Main backend API gateway — accounts, OAuth, machine tokens, LLM proxy, inbox, vault. Wired together for the universal-bot path.",
  "main": "./src/server.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "dedup-credentials": "node dist/scripts/dedup-credentials.bin.js",
    "dedup-credentials:apply": "node dist/scripts/dedup-credentials.bin.js --apply"
  },
  "dependencies": {
    "@fastify/cookie": "^9.4.0",
    "@fastify/rate-limit": "^9.1.0",
    "@prisma/client": "^5.22.0",
    "@trusty-squire/adapter-sdk": "workspace:*",
    "@trusty-squire/inbox": "workspace:*",
    "@trusty-squire/vault": "workspace:*",
    "bullmq": "^5.21.1",
    "fastify": "^4.28.0",
    "imapflow": "^1.3.3",
    "jose": "^5.9.6",
    "nodemailer": "^8.0.7",
    "pino": "^9.5.0",
    "ulid": "^2.3.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.0",
    "@vitest/coverage-v8": "^1.6.1",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
