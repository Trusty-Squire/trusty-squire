{
  "name": "@trusty-squire/harvester",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Skill harvester — drives provision_any_service across a curated queue of dev SaaS services, reports per-service progress to GitHub issues.",
  "scripts": {
    "dry-run": "node run.mjs --dry-run",
    "once": "node run.mjs --once",
    "digest": "node daily-digest.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "vitest": "^1.6.1"
  }
}
