node_modules/
dist/
.next/
build/
out/

.env
.env.local
.env.*.local

*.tsbuildinfo
*.log
*.tgz
coverage/
.nyc_output/

.DS_Store
Thumbs.db
.idea/
.vscode/settings.json
.vscode/launch.json

.turbo/
.cache/

# Demo orchestrator runtime
.demo-logs/
.demo-pids/
.demo-urls/

# Local binaries / installers
cloudflared.deb

# Universal signup bot debug artifacts
.debug/
debug-*.html
debug-*.png

# Personal Claude Code settings (per-developer, not shared)
.claude/settings.local.json

# Raw onboarding-round captures (curate into the eval corpus)
.onboarding-corpus-raw/
.gstack/

# npm lockfile — repo uses pnpm. npm lockfiles sneak in when
# `npm install` runs ad-hoc inside a workspace; they're never
# authoritative here and shouldn't be committed.
package-lock.json

# CodeGraph local index — per-developer, regenerated from source.
.codegraph/
